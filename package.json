{
  "name": "bibliomancy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test",
    "check": "biome check . && bunx tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "bun-types": "^1.3.9"
  }
}
